import React from 'react';
import ReactDOM from 'react-dom';

import { BomDia, BoaNoite } from './componentes/multiplos';

ReactDOM.render( < div > < BomDia nome = 'André' / > < BoaNoite nome = 'Joana' / > < /div > , document.getElementById('root'));